import MainContent from "../components/MainContent";
import Wrapper from "../components/Wrapper";

const Home: React.FC = () => {
  return (
    <Wrapper>
      <MainContent />
    </Wrapper>
  );
};

export default Home;
